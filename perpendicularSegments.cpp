#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int X, Y, K;
        cin >> X >> Y >> K;

        cout << 0 << " " << 0 << " " << X << " " << Y << "\n";

        cout << 0 << " " << Y << " " << Y << " " << 0 << "\n";
    }
    return 0;
}
